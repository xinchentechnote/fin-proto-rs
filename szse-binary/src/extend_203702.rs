// Code generated by fin-protoc. DO NOT EDIT.
use binary_codec::*;
use bytes::{Bytes, BytesMut};

#[derive(Debug, Clone, PartialEq)]
pub struct Extend203702 {
    pub cash_margin: char,
}

impl BinaryCodec for Extend203702 {
    fn encode(&self, buf: &mut BytesMut) {
        put_char(buf, self.cash_margin);
    }

    fn decode(buf: &mut Bytes) -> Option<Extend203702> {
        let cash_margin = get_char(buf)?;
        Some(Self { cash_margin })
    }
}

#[cfg(test)]
mod extend_203702_tests {
    use super::*;
    use bytes::BytesMut;

    #[test]
    fn test_extend_203702_codec() {
        let original = Extend203702 { cash_margin: 'a' };

        let mut buf = BytesMut::new();
        original.encode(&mut buf);
        let mut bytes = buf.freeze();

        let decoded = Extend203702::decode(&mut bytes).unwrap();

        assert_eq!(original, decoded);
    }
}
