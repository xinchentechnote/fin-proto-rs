// Code generated by fin-protoc. DO NOT EDIT.
use binary_codec::*;
use bytes::{Buf, BufMut, Bytes, BytesMut};

#[derive(Debug, Clone, PartialEq)]
pub struct Extend100201 {
    pub stop_px: i64,
    pub min_qty: i64,
    pub max_price_levels: u16,
    pub time_in_force: String,
}

impl BinaryCodec for Extend100201 {
    fn encode(&self, buf: &mut BytesMut) {
        buf.put_i64(self.stop_px);
        buf.put_i64(self.min_qty);
        buf.put_u16(self.max_price_levels);
        put_char_array(buf, &self.time_in_force, 1);
    }

    fn decode(buf: &mut Bytes) -> Option<Extend100201> {
        let stop_px = buf.get_i64();
        let min_qty = buf.get_i64();
        let max_price_levels = buf.get_u16();
        let time_in_force = get_char_array(buf, 1)?;
        Some(Self {
            stop_px,
            min_qty,
            max_price_levels,
            time_in_force,
        })
    }
}

#[cfg(test)]
mod extend_100201_tests {
    use super::*;
    use bytes::BytesMut;

    #[test]
    fn test_extend_100201_codec() {
        let original = Extend100201 {
            stop_px: -123456789,
            min_qty: -123456789,
            max_price_levels: 1234,
            time_in_force: vec!['a'; 1].into_iter().collect::<String>(),
        };

        let mut buf = BytesMut::new();
        original.encode(&mut buf);
        let mut bytes = buf.freeze();

        let decoded = Extend100201::decode(&mut bytes).unwrap();
        assert_eq!(original, decoded);
    }
}
